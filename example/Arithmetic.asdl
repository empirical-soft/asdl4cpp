-- Example ASDL for defining abstract syntax for arithmetic expressions

module Arithmetic
{
  expr = BinOp(expr left, operator op, expr right)
       | Num(int n)
       
  operator = Add | Sub | Mul | Div
}
